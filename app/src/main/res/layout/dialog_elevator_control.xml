<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="450dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    >

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/elevator_control_dialog_title"
        android:textSize="23sp"
        android:textStyle="bold"
        android:layout_marginBottom="20dp"
        android:gravity="center"/>

    <!-- 圆形控制按钮 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="7dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnOpenDoor"
            android:layout_width="0dp"
            android:layout_height="90dp"
            android:layout_weight="1"
            android:layout_marginHorizontal="6dp"
            android:text="@string/open_elevator"
            app:cornerRadius="15dp"
            android:textSize="20sp"
            app:backgroundTint="#2196F3"
            android:textColor="@android:color/white"
            android:maxLines="2"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnCloseDoor"
            android:layout_width="0dp"
            android:layout_height="90dp"
            android:layout_weight="1"
            android:layout_marginHorizontal="6dp"
            android:text="@string/close_elevator"
            app:cornerRadius="15dp"
            app:backgroundTint="#2196F3"
            android:textSize="20sp"
            android:textColor="@android:color/white"
            android:maxLines="2"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnClearOccupy"
            android:layout_width="0dp"
            android:layout_height="90dp"
            android:layout_weight="1"
            android:layout_marginHorizontal="6dp"
            android:text="@string/clear_occupy"
            app:cornerRadius="15dp"
            android:textSize="20sp"
            app:backgroundTint="#2196F3"
            android:textColor="@android:color/white"
            android:maxLines="2"/>
    </LinearLayout>
</LinearLayout>
